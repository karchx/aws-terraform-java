version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    networks:
      - app
    ports:
      - "8080:8080"
    volumes:
      - .:/usr/src/app
    
networks:
  app: {}